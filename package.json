{
  "name": "token-bonding-curves",
  "version": "1.0.0",
  "description": "Fungible token with extensibility and bonding curves",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "prettier --write **.sol",
    "solhint": "solhint --config ./.solhint.json 'src/**/*.sol' --fix",
    "lint": "npm run prettier && npm run solhint"
  },
  "author": "Guillermo Fairbairn",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.8.4",
    "prettier-plugin-solidity": "^1.1.3",
    "solhint": "^3.4.1"
  }
}
